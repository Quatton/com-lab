理学部情報科学科　J4220661
タエチャスクナッチャノン

(define a 100)
;a = 100
(define h 
  (lambda (x) 
    (lambda (y) 
      (+ x 
        (*  a ;この a はグローバルの閉包を用い、100として評価される。
            y
        )
      )
    )
  )
)

(let 
  ((a 0))

  ((h 1) ;上述したように、hの内部なら、lambda (y) (1 + (100 * y))として評価される。
    
    (+ a 5); letの閉包内部なら、a = 0 として評価される

  ; (+ a 5) = 5 を(h 1)の約数としてyに代入する。
  ;(+ 1 (* 100 5)) 
  ;(+ 1 500)
  ;501
  
))